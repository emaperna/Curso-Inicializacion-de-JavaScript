// En JavaScript, los tipos de datos nos permiten conocer las características y funcionalidades que estaran disponible para este dato.

// Los tipos de datos en JavaScript son:

// 1. Number (Representacion de numeros enteros o decimal)

// Ejemplo:

let edad = 32;
let precio = 150.65; // Punto para separa las decimas de las unidades, porque JavaScript esta escrito en ingles.

// 2. String (Representacion de texto alfanumerico que puede tener cualquier longitud, con el uso de comillas simples ' o dobles " )

// Ejemplo:

let nombre = "Juan";

// 3. Boolean (Representacion de un valor booleano, que puede ser verdadero o falso)

// Ejemplo:

let esPerro = true;
let esHumano = false;

// Estos datos booleanos no se encierran entre comillas,porque si asi lo hicieramos cambiariamos de valor a string.

// Array: Los arrays son colecciones de datos. Los podemos reconocer porque se declaran con corchetes [ ]. 

// Ejemplo:

let comidaFavorita = ["milanesa", "pizza", "pastas"];
let numerosSorteados = [12, 45, 56, 324, 452];